name: Build Flutter APK

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'

      - name: Configure Android & Build APK
        run: |
          flutter create . --platforms=android
          flutter pub add hive hive_flutter receive_sharing_intent path_provider permission_handler http
          flutter pub get
          
          sed -i 's/compileSdk = .*/compileSdk = 37/' android/app/build.gradle* || true
          sed -i 's/compileSdkVersion .*/compileSdkVersion 37/' android/app/build.gradle* || true

          MANIFEST_PATH="android/app/src/main/AndroidManifest.xml"
          PERMISSIONS='<uses-permission android:name="android.permission.INTERNET"/><uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/><uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>'
          sed -i "s|<manifest .*|&\n    $PERMISSIONS|" $MANIFEST_PATH

          INTENT_FILTER='<intent-filter><action android:name="android.intent.action.SEND"/><category android:name="android.intent.category.DEFAULT"/><data android:mimeType="text/plain"/></intent-filter></activity>'
          sed -i "s|</activity>|$INTENT_FILTER|g" $MANIFEST_PATH

          flutter build apk --release

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: release-apk
          path: build/app/outputs/flutter-apk/app-release.apk
